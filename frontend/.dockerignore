# SECURITY: build context is frontend/ (see docker/docker-compose.yml) —
# keep secrets, deps and local build output out of the image context
.env
*.env
.env.*
!.env.example
node_modules/
.next/
out
npm-debug.log
.git/
.gitignore
